services:
  bento-pdf:
    image: bentopdf/bentopdf:latest
    container_name: bento-pdf
    restart: unless-stopped
    environment:
      - TZ=${TZ:-Asia/Jakarta}
    networks:
      - homelab
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.bento-pdf.rule=Host(`bento-pdf.${TRAEFIK_DOMAIN:-yourdomain.com}`)"
      - "traefik.http.routers.bento-pdf.entrypoints=websecure"
      - "traefik.http.routers.bento-pdf.tls=true"
      - "traefik.http.routers.bento-pdf.middlewares=secure-headers@docker"
      - "traefik.http.routers.bento-pdf.service=bento-pdf-svc"
      - "traefik.http.routers.bento-pdf-local.rule=Host(`bento-pdf.${LOCAL_DOMAIN:-local}`)"
      - "traefik.http.routers.bento-pdf-local.entrypoints=websecure"
      - "traefik.http.routers.bento-pdf-local.tls=true"
      - "traefik.http.routers.bento-pdf-local.middlewares=secure-headers@docker"
      - "traefik.http.routers.bento-pdf-local.service=bento-pdf-svc"
      - "traefik.http.services.bento-pdf-svc.loadbalancer.server.port=8080"
    deploy:
      resources:
        limits:
          cpus: "${BENTO_PDF_CPU_LIMIT:-0.5}"
          memory: ${BENTO_PDF_MEMORY_LIMIT:-256M}

networks:
  homelab:
    external: true