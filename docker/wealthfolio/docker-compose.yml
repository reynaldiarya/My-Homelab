services:
  wealthfolio:
    image: afadil/wealthfolio:latest
    container_name: wealthfolio
    restart: unless-stopped
    ports:
      - "${BIND_IP}:${WEALTHFOLIO_PORT:-8094}:8088"
    environment:
      WF_LISTEN_ADDR: 0.0.0.0:8088
      WF_DB_PATH: /data/wealthfolio.db
      WF_SECRET_KEY: cAWpMFYyG08cdfSp0slkvzIuUzWZUOhEqrO8ooHDdvM=
    volumes:
      - wealthfolio_data:/data
    networks:
      - homelab
    deploy:
      resources:
        limits:
          cpus: "${WEALTHFOLIO_CPU_LIMIT:-0.5}"
          memory: ${WEALTHFOLIO_MEMORY_LIMIT:-512M}

networks:
  homelab:
    external: true

volumes:
  wealthfolio_data:
